package com.alergush.flutter_caller.utils;

public enum CallMethods {
    register,
    unregister,
    answer,
    hangup,
    checkCallState,
    toggleMute,
    toggleSpeaker,
    checkPermissions,
}
